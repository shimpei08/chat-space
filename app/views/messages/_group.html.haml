%li.sidebar__bottom__list
  = link_to new_group_message_path(group) do
    %h3
      = group.name
    %p
      = group.messages.last.try(:body)
      - unless group.messages.exists?
        まだメッセージがありません。
