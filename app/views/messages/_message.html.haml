- @comment = group.messages.last
%li.sidebar__bottom__list
  = link_to new_group_message_path(group.id) do
    %h3
      = group.name
    %p
      -if group.messages.exists?
        = @comment.body
      -else
        まだメッセージはありません。
